# ============================================================
# MedSupply AI - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values.
# The app will work out of the box with defaults (SQLite fallback).

# ---- Database (PostgreSQL) ---------------------------------
# Set USE_SQLITE_FALLBACK=false once Postgres is running.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medsupply
DB_USER=medsupply_user
DB_PASSWORD=medsupply_pass
# Or provide a full URL (overrides individual settings above):
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
USE_SQLITE_FALLBACK=true

# ---- Google Sheets API ------------------------------------
# Path to the service-account JSON key file
GOOGLE_SHEETS_CREDENTIALS_FILE=credentials/google_service_account.json
# Comma-separated Google Sheet IDs to pull data from
GOOGLE_SHEET_IDS=

# ---- Pipeline Scheduler -----------------------------------
PIPELINE_CRON_DAY=mon
PIPELINE_CRON_HOUR=2
PIPELINE_CRON_MINUTE=0

# ---- Retraining -------------------------------------------
# Minimum new weekly records to trigger model retraining
RETRAIN_THRESHOLD=50
# How many old model versions to keep on disk
MODEL_RETENTION_COUNT=5

# ---- File Ingestion ----------------------------------------
# Directory to watch for new CSV/Excel uploads
# INGEST_WATCH_DIR=data
# Original seed dataset
# SEED_CSV=uganda_drug_supply_synthetic.csv

# ---- FastAPI -----------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000

# ---- Logging -----------------------------------------------
LOG_LEVEL=INFO
